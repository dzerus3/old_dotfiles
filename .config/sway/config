# Dependencies
# To make full use of this config file, you need the following:
## A cursor theme called "current"
## kitty (Terminal Emulator)
## j4-dmenu-desktop (Gets .desktop files)
## bemenu (Application launcher)
## mako (Notification center)
## gentoo-pipewire-launcher (Audio)
## fcitx (Chinese language input)
## Waybar version > 0.9.17 (For status bar)

# Variables
## Windows key
set $mod Mod4

## Home row direction keys, like vim
set $left n
set $down e
set $up i
set $right o

## Screenshot folder
set $psf $(xdg-user-dir PICTURES)/ps_$(date +"%Y%m%d%H%M%S").png

## Preferred terminal emulator
set $term kitty

# Applications

## Polkit, so GUIs can ask for elevated privileges
#exec /usr/bin/lxpolkit

## Notification center, so notifications are displayed graphically
exec /usr/bin/mako

## Gentoo launcher for pipewire
exec /usr/bin/gentoo-pipewire-launcher

## The application launcher menu
set $menu j4-dmenu-desktop --dmenu='bemenu --tf "#5D8CAE" --nb "#272727" --nf "#c0c0c0" --hb "#1a6b3c" --hf "#FFFFFF" -i --fn "Deja Vu Sans Mono 10"' --term='kitty'

# Configuration
## Disable window titlebars
default_border pixel
default_floating_border pixel

## Add gaps between windows
gaps inner 8
gaps outer 2

## Cursor theme
seat seat0 xcursor_theme current 16

## Wallpaper
#set $wallpapers_path $(xdg-user-dir PICTURES)/wallpaper
#output * bg $(find $wallpapers_path -type f | shuf -n 1) fill
output * bg $HOME/pics/wallpaper/wallpaper.jpg fill

## Idle configuration
exec swayidle -w \
         timeout 1800 'swaylock -f -c 000000' \
         timeout 2700 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"'
         # This options doesn't work without elogind or systemd
         #before-sleep 'swaylock -e -f -c 000000'

## Keyboard configuration
input type:keyboard {
    xkb_layout  "us,es,ru"
    #xkb_file    "~/.config/xkb/custom"
    xkb_variant "colemak_dh,colemak_dh,rulemak_dh"
    xkb_options "caps:escape,grp:win_space_toggle"
}

# Key bindings
## Start a terminal
bindsym $mod+Return exec $term

## Kill focused window
bindsym $mod+w kill

## Start your launcher
bindsym $mod+d exec $menu

## Drag floating windows by holding down $mod and left mouse button.
## Resize them with right mouse button + $mod.
floating_modifier $mod normal

## Screenshots
bindsym Print               exec grim - | wl-copy
bindsym Shift+Print         exec grim -g "$(slurp)" - | wl-copy
bindsym Control+Print       exec grim $psf
bindsym Control+Shift+Print exec grim -g "$(slurp)" $psf

## Reload the configuration file
bindsym $mod+Shift+c reload

## Exit sway (logs you out of your Wayland session)
# bindsym $mod+Shift+q exec echo 'yes\nno' | bemenu --tf "#5D8CAE" --nb "#272727" --nf "#c0c0c0" --hb "#1a6b3c" --hf "#FFFFFF" -p "Are you sure you want to exit?" | grep yes && swaymsg exit
bindsym $mod+Shift+q exec $HOME/.local/bin/sway/sway-logout.sh

## Moving around:
### Move your focus around
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

### up|down|left|right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

## Workspaces:
### Switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

### Move focused window to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

### Moves window to next/previous workspace, and switches to it
bindsym $mod+control+$right move container to workspace next, workspace next
bindsym $mod+control+$left move container to workspace prev, workspace prev

## Layout
### Move the focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

### Ditto, with arrow keys
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

### Splits window vertically/horizontally
### In other words, the next windows you open will be that orientation
bindsym $mod+z splith
bindsym $mod+x splitv

### Make the current window fullscreen
bindsym $mod+f fullscreen

### Swap focus between the tiling area and the floating area
bindsym $mod+t focus mode_toggle
### Toggle the current window between tiling and floating mode
bindsym $mod+Shift+t floating toggle

### Switch the current container between different layout styles
bindsym $mod+l layout stacking
bindsym $mod+u layout tabbed
bindsym $mod+y layout toggle split
# Move focus to the parent container
# No idea what this actually does
#bindsym $mod+j focus parent

# Scratchpad:
## Move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

## Show the next scratchpad window or hide the focused scratchpad window.
## If there are multiple scratchpad windows, this command cycles through them.
bindsym $mod+minus scratchpad show

# Resizing containers:
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# See .config/waybar/config for configuration
bar swaybar_command waybar

include /etc/sway/config.d/*
